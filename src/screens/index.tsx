import React,{useState, useCallback} from 'react';
import {View,Text,StyleSheet,Button} from 'react-native';

const IndexScreen = () => {
    return (
        <View>
            <Button title='dsadsadsa2222 :O'></Button>
        </View>
    );
};

const styles = StyleSheet.create({
    home: {
        flex: 1,
        alignItems: 'center'
    }
});

export default IndexScreen;